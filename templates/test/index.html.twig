{% extends 'base.html.twig' %}



{% block body %}
<div class="jumbotron">
    <div class="well">
        <h1>Hello
            {% if is_granted('ROLE_ADMIN') %}
            {{ app.user.prenom}} {{ app.user.nom}}</h1>
        {% endif %}
        <div class="row">
            <div class="col-md-4">
                <a href="{{ path('security_login') }}"><button type="button" class="btn btn-primary">Connexion</button></a>
            </div>
            {% if is_granted('ROLE_ADMIN') %}
                <div class="col-md-4">
                    <a href="{{ path('user_registration') }}"><button type="button" class="btn btn-success">inscription</button></a>
                </div>
                <div class="col-md-4">
                    <a href="{{ path('admin') }}"><button type="button" class="btn btn-primary">admin</button></a>
                </div>
            {% endif %}
        </div>
    </div>
</div>


{% endblock %}
